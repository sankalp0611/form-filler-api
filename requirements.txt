# Core API + Server
fastapi
uvicorn
python-multipart

# HTML + PDF Parsing
beautifulsoup4
pytesseract
pdf2image
PyMuPDF
pdfkit
pdfplumber
python-dotenv

# LangChain + Groq
langchain
langchain-groq
langchain-community

# Embeddings + Transformers
sentence-transformers==2.4.0
InstructorEmbedding==1.0.1
transformers==4.32.0  # SINGLE DECLARATION
huggingface-hub>=0.30.0,<1.0
tqdm
scikit-learn
numpy>=2.3.1  # PREVENT LEGACY VERSION CONFLICT

# Torch (CPU-only)
torch==2.5.0  # SINGLE DECLARATION

# FAISS (CPU)
faiss-cpu==1.7.4  # PIN COMPATIBLE VERSION
