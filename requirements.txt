# === Core API + Server ===
fastapi
uvicorn
python-multipart
python-dotenv

# === HTML + PDF Parsing ===
beautifulsoup4
pytesseract
pdf2image
PyMuPDF
pdfkit
pdfplumber

# === LangChain + Groq ===
langchain
langchain-groq
langchain-community

# === Embeddings + Transformers ===
sentence-transformers==2.4.0
InstructorEmbedding==1.0.1
transformers==4.32.0
huggingface-hub>=0.30.0,<1.0
tqdm
scikit-learn
numpy

# === Torch (CPU only, compatible) ===
torch==2.5.1

# === FAISS backend (Render = CPU only) ===
faiss-cpu
